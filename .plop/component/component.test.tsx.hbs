import { {{pascalCase name}} } from './{{pascalCase name}}'
import { renderWithRouter } from '~/test.util'

describe('<{{pascalCase name}} />', () => {
  it('should renders correctly', () => {
    renderWithRouter(<{{pascalCase name}}>Test</{{pascalCase name}}>)
  })
})
